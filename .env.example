# ============================================================================
# Warehouse Management Application - Environment Configuration
# ============================================================================
# 
# This file contains all configurable environment variables for the application.
# Copy this file to .env and fill in the values for your environment.
#
# IMPORTANT: Never commit .env file to version control!
#
# Usage:
#   1. Copy this file: cp .env.example .env
#   2. Edit .env with your specific values
#   3. Restart the application
#
# ============================================================================

# ----------------------------------------------------------------------------
# Application Settings
# ----------------------------------------------------------------------------

# Application name (displayed in window title and logs)
APP_NAME=Warehouse Management

# Application version (semantic versioning: MAJOR.MINOR.PATCH)
APP_VERSION=2.0.0

# Environment: dev, staging, or production
# - dev: Development mode, no license required, debug logging enabled
# - staging: Testing mode, optional license, standard logging
# - production: Production mode, license required, optimized logging
ENVIRONMENT=dev

# ----------------------------------------------------------------------------
# File Paths
# ----------------------------------------------------------------------------

# Base directory for the application (leave empty to use current directory)
APP_BASE_DIR=

# Database file path (relative to APP_BASE_DIR or absolute path)
# SQLite database file for storing products, sessions, and history
DB_PATH=storage.db

# Export directory for generated reports (relative or absolute)
EXPORT_DIR=exports

# Backup directory for database backups (relative or absolute)
BACKUP_DIR=backups

# Log directory for application logs (relative or absolute)
LOG_DIR=logs

# ----------------------------------------------------------------------------
# User Interface Settings
# ----------------------------------------------------------------------------

# Default window width in pixels
WINDOW_WIDTH=1200

# Default window height in pixels
WINDOW_HEIGHT=800

# UI theme: modern, classic, or dark
THEME=modern

# ----------------------------------------------------------------------------
# Server Configuration
# ----------------------------------------------------------------------------

# Port for bank notification HTTP server
# Default: 5005 (change if port is already in use)
NOTIFICATION_PORT=5005

# Host address for notification server
# 0.0.0.0 = listen on all network interfaces
# 127.0.0.1 = listen only on localhost
NOTIFICATION_HOST=0.0.0.0

# Enable SSL/TLS for notification server (true/false)
# Requires SSL certificate configuration
ENABLE_SSL=false

# ----------------------------------------------------------------------------
# Security Settings
# ----------------------------------------------------------------------------

# License key for commercial distribution (REQUIRED for production)
# Format: <base64_payload>.<base64_signature>
# Contact support to obtain a valid license key
LICENSE_KEY=

# Encryption key for sensitive data (optional)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
ENCRYPTION_KEY=

# ----------------------------------------------------------------------------
# Logging Configuration
# ----------------------------------------------------------------------------

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# - DEBUG: Detailed diagnostic information (development only)
# - INFO: General informational messages (recommended for production)
# - WARNING: Warning messages for unexpected situations
# - ERROR: Error messages for failures
# - CRITICAL: Critical errors that may cause shutdown
LOG_LEVEL=INFO

# Log rotation strategy: daily, size, or none
# - daily: Rotate logs daily at midnight
# - size: Rotate when log file reaches max size
# - none: No rotation (not recommended for production)
LOG_ROTATION=daily

# Maximum log file size in MB (only used when LOG_ROTATION=size)
# LOG_MAX_SIZE=10

# Number of backup log files to keep
# LOG_BACKUP_COUNT=5

# ----------------------------------------------------------------------------
# Advanced Settings (Optional)
# ----------------------------------------------------------------------------

# Database connection pool size (for future multi-threading support)
# DB_POOL_SIZE=5

# Request timeout in seconds for HTTP operations
# REQUEST_TIMEOUT=30

# Maximum number of concurrent notification requests
# MAX_CONCURRENT_REQUESTS=10

# Enable performance profiling (true/false)
# ENABLE_PROFILING=false

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
#
# 1. Environment Variables Priority:
#    - System environment variables override .env file values
#    - .env file values override application defaults
#
# 2. Security Best Practices:
#    - Never commit .env file to version control
#    - Use strong encryption keys (32+ characters)
#    - Rotate license keys periodically
#    - Use HTTPS in production (ENABLE_SSL=true)
#
# 3. Troubleshooting:
#    - If application fails to start, check LOG_DIR for error logs
#    - Verify all required directories exist and are writable
#    - Ensure LICENSE_KEY is valid for production environment
#
# 4. Support:
#    - For license issues: contact support@example.com
#    - For technical issues: check logs in LOG_DIR
#    - Documentation: see README.md
#
# ============================================================================
